{
  "name": "bazel-actions",
  "version": "0.0.1",
  "description": "Run bazel targets and set the checks",
  "main": "index.mjs",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "ncc build index.mjs -s -o dist/main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Remi Cattiau <rcattiau@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@vercel/ncc": "^0.38.0",
    "pretty-ms": "^9.1.0"
  }
}
